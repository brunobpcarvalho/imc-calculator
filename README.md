# imc-calculator
Calculadora de IMC para uma academia.

### 1. Create docker .env

```bash
$ cd .docker/  
$ cp .env.example .env
```

### 3. Initialize docker services:

```bash
$ make start
$ sudo make build
$ make npm-install
```